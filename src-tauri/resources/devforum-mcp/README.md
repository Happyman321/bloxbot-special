# Roblox DevForum MCP bundle

`server.mjs`, `LICENSE`, and dependency license files are generated by
`pnpm build:devforum-mcp` from the exact `roblox-devforum-mcp` version pinned
in the root package manifest. They are ignored by Git and included in BloxBot
installers as Tauri resources.
